import './ElementPage'
import React from 'react';
import { useParams } from 'react-router-dom';

function ElementPage(maxWidth760={maxWidth760}) {
    const { id } = useParams();
    // const item = getCatalogItemById(id);
    return (
        <>
        
        </>
    )
}

export default ElementPage


